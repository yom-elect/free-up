#functions rigkikrgigoglogn okg lknblggntg;egng gnegnlgngjsgnwqadeupiyptan ioruiyomi
from random import random
'''def number_first_squared ():
    user = input ("whats your age?: ")
    int (user)
    return user
result = number_first_squared ()
print (result)

def flip_coin():
    while True:
     if random() > 0.5:
        return "Heads"
     else:
        return "tails"
print (flip_coin())

def planet(name):
    print (f"here i am {name}")
planet ('name')

def divide ():
    user = input ("enter first number:  ")
    user2 = input ("enter second number:  ")
    return float (user)/ float (user2)
result= divide ()
print (result)

def sum_even_numbers (number):
    total = 0
    for num in number:
        if num % 2 == 0:
            total += num
    return total
print (sum_even_numbers([1,2,3,4,5,6,7,8])) 

#default parameter usually at the end of function parameter
#scope
#global and local scope 
planet = 340

def p_p_l():
    global planet
    planet += 4
    return planet
p_p_l()
print (planet)

def f (x,l=[]):
    for i in range(x):
     l.append(i*i)
     print (l)
f(2)
f(3,[1,2,3])
f(3)
#Built in functions not fun at all, need inspiration 
def multi_built_in():
    user = ['yomi3elct', 'timothy' , 'kofo']
    me = list(map(lambda user: f"Your instructor is {user}", filter(lambda u: len(u)<7, user)))
    return me 
print (multi_built_in()
list comprehension is by far better than these'''